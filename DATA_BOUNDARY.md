# 数据与发布边界

本仓库以公开科研代码、配置和**团队自有训练产物**为主。  
**修改本协议不能取消第三方数据集 / 预训练模型的原许可证义务。**

最后修订：2026-08-09（允许上传自有权重与合规训练产物；第三方原始数据仍禁止再分发）

---

## 一句话原则

| 类别 | 能否进本公开 GitHub |
|---|---|
| 你们自己写的代码、配置、合成测试 | 可以 |
| 你们训练得到的 **ComposerN3 / N3 自有 checkpoint**（不含第三方数据正文） | **可以**（建议放 `模型/artifacts/`，大文件用 Git LFS） |
| 不含可识别个体的聚合指标、曲线、科研图 | 可以 |
| MELD / EmotionTalk / IEMOCAP 等的**原始或再分发**数据 | **不可以**（原许可限制，不是“改协议就能传”） |
| 从上述数据抽出的、可还原样本的特征 / 逐条预测表 | **不可以**（除非你已获得书面再分发授权并在此备案） |
| 第三方预训练权重原样打包上传（如完整 XLM-R / WavLM 文件） | **不建议**；请协作者按官方入口自行下载（许可证通常允许使用，但不等于鼓励把整包镜像进本仓） |
| 密钥、Cookie、私钥、授权邮件原文 | **不可以** |

---

## 可以进入仓库

- 自行编写的实验代码、合同测试和冻结配置；
- 官方下载脚本、固定 revision、字节数和 SHA-256 校验清单（不含数据正文）；
- 不含单条样本身份的聚合统计、训练曲线摘要、科研图；
- **团队自有模型权重 / checkpoint**：例如在本仓库 `模型/` 下训练得到的 N3 参数文件（`.pt` / `.pth` / `.safetensors`），须附：
  - 对应配置 ID / commit；
  - 训练数据**角色说明**（如仅用 train/dev，未使用封存 test）；
  - 所用第三方预训练塔的 **model_id + 许可证名称 + 官方下载方式**（不要把第三方整包权重当“自有数据”重传，除非许可证明确允许且团队选择承担体积与合规责任）；
- 由自有训练产生的、已脱敏的日志摘要与指标 JSON（非逐查询、非说话人可还原表）。

推荐目录：

```text
模型/artifacts/          # 自有最终/候选 checkpoint 与指标卡
releases/notes/          # 版本说明（可选）
```

单文件超过约 100MB 时使用 **Git LFS**。

---

## 仍然禁止进入仓库

以下禁令来自**第三方许可与合规**，本协议修订**不能**使其合法：

- EmotionTalk、MELD、IEMOCAP、CPED、M3ED 的原始或重分发文本、标签、音频、视频和压缩包；
- 可还原单条样本的逐查询结果、说话人/对话键、转写全文表、媒体索引；
- 从上述受限数据导出的、实质上等于再分发数据的特征库（`.npz` / 大规模 `.npy` 特征仓等），**除非**已取得书面再分发授权并在本文件“授权备案”节登记；
- IEMOCAP release form、DUA、伦理批复邮件原文、机构未公开材料；
- GitHub / Hugging Face token、密码、Cookie、私钥、`.env` 密钥。

---

## 关于“训练出来的东西能不能上传”

| 训练产物 | 一般能否上传到本仓 |
|---|---|
| 自有网络的权重（N3 头、门控、投影等） | **可以**（按上文自有 checkpoint 规则） |
| 损失曲线、Accuracy / F1 等聚合结果 | **可以** |
| 超参数与冻结配置 JSON | **可以** |
| 训练集/测试集原文、音视频拷贝 | **不可以**（第三方数据） |
| 带说话人 ID 的逐条预测大表 | **不可以**（可识别 / 近乎数据再分发） |
| 合成（synthetic）合同测试数据 | **可以**（本仓库已有此类用途） |

**结论：** 训练出的是“你们模型的本事”（权重）和“汇总分数”→ 可以按新协议上传；训练时用到的**原始数据集本身**→ 仍然不能上传。

---

## 授权备案（可选）

若未来依法取得某数据集的**再分发**书面许可，在下方登记后再放开对应特征/子集上传，并同步修改 `.gitignore` 白名单：

| 日期 | 数据/特征范围 | 授权方 | 文档存放位置（勿提交密钥） | 经办人 |
|---|---|---|---|---|
| （空） |  |  |  |  |

---

## 数据获取（原始数据仍走官方）

研究者必须从数据集官方入口自行申请或下载，并独立遵守许可、署名、使用和再分发条件。协作者使用 [`datasets/`](datasets/) 中的固定版本与校验清单在**仓库外**建立本地数据目录。

仓库的 MIT License 只覆盖本仓库自行编写的代码、文档和**团队自有训练产物的发布方式约定**；不改变第三方数据、预训练模型或媒体的许可证。
