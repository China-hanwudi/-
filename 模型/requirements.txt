# ComposerN3 training dependencies (synthetic smoke is enough with torch+numpy).
numpy>=1.24
torch>=2.1
pytest>=7.0

# Optional: enable text_tower=xlm_roberta_large (weights download locally, never commit)
# transformers>=4.44
# safetensors>=0.4
# sentencepiece>=0.1.99
